{
  "name": "tomato-focus",
  "version": "0.0.1",
  "description": "Pomodoro timer with text editor and gamification",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "babel -d electron/react/ --presets @babel/preset-react src/ && electron --no-sandbox ."
  },
  "author": "Brooks McKinley",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.1.0",
    "@babel/core": "^7.1.0",
    "@babel/preset-env": "^7.1.0",
    "@babel/preset-react": "^7.0.0",
    "babel-preset-react-app": "^3.1.2",
    "electron": "^12.0.2"
  }
}
